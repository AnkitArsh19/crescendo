# API base URL — used by axios.js for all backend requests.
# Set this to your backend's URL for the target environment.
#
# Local development:
VITE_API_URL=http://localhost:8080
#
# Production (default fallback if not set):
# VITE_API_URL=https://api.crescendo.run
